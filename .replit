# Replit configuration file
# Copy this to .replit in your Replit project

run = "python main.py"
entrypoint = "main.py"
[languages]
python3 = "3.11"

[deploy]
run = ["sh", "-c", "gunicorn main:app --bind 0.0.0.0:$PORT --workers 1 --timeout 3600"]
